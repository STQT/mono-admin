import { ContactSettingsTable } from "@/components/contact-settings/contact-settings-table"

export const metadata = {
  title: "Admin kontaktlar — Mona Admin",
}

export default function ContactSettingsPage() {
  return <ContactSettingsTable />
}
