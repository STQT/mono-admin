import { UsersTable } from "@/components/auth-admin/users-table"

export const metadata = {
  title: "Admin foydalanuvchilar — Mona Admin",
}

export default function AdminUsersPage() {
  return <UsersTable />
}
