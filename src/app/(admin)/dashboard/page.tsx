import { DashboardView } from "@/components/dashboard/dashboard-view"

export const metadata = {
  title: "Bosh sahifa — Mona Admin",
}

export default function DashboardPage() {
  return <DashboardView />
}
