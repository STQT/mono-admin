import { LiveStreamsTable } from "@/components/live-streams/live-streams-table"

export const metadata = {
  title: "Jonli efirlar — Mona Admin",
}

export default function LiveStreamsPage() {
  return <LiveStreamsTable />
}
